<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:batch="http://www.mulesoft.org/schema/mule/batch" xmlns:http="http://www.mulesoft.org/schema/mule/http" xmlns="http://www.mulesoft.org/schema/mule/core">
    <http:listener-config name="HTTP_Listener_config">
        <http:listener-connection host="0.0.0.0" port="8081" />
    </http:listener-config>
    
    <flow name="api-flow">
        <http:listener path="/api/hybrid" config-ref="HTTP_Listener_config"/>
        <logger level="INFO" message="API Request Received"/>
    </flow>

    <flow name="batch-flow">
        <scheduler>
            <scheduling-strategy>
                <fixed-frequency frequency="1000"/>
            </scheduling-strategy>
        </scheduler>
        <batch:job jobName="syncRecords">
             <!-- Batch logic -->
        </batch:job>
    </flow>
</mule>
